/*import java.io.*;
import java.util.*;


public class Mystack2{
    private LinkedList<string> data;

public MyStack(){
  data = new LInkedList<String>();
}

public void push(String value){
  data.addFrist (value);

}

public String pop() {
// add code to remove and return the
// item on the top of the stack
  String v = data.removeFirst();
  return v;
}

public Sting top(){
//add code to return but not remove the
//item on the top of the stack
return data.get(0);
}

Stack<String> pancakes = new Stack<String>();

*/

public class MyStack2{
 Stack<String> originalstack;


//public MyStack2 (String s){
//originalstack = new Stack<Character>;
//  for (int i= 0 ; 1< s.length() ; i++){
//    originalStack.push(s.charAt(i));
  }

  }





}

  public reverse(String s){
        originalstack = new Stack<Character>;
    String revstring  = null;
    int             i = 0;
    for (int i = 0 ; i < s.length() ; i++){

    }

    while (!s.charAt(i).isEmpty()){
    revstring += originalstack.pop(charAt(i));
    i++;
    } //  endwhile


  //sets up a for loop where i matches the index
  //of the character we are looking at. It starts
  //at the end of the string and goes to the front
//  int length = s.length();

//  for (int i = 0 ; i <= s.length ; i++ ){
//    revstring += originalStack.pop(charAt(i));
//  }
//  return revstring;
//  }









}
